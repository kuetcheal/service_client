route:
  receiver: "console"
  group_by: ["alertname", "job"]
  group_wait: 10s
  group_interval: 2m
  repeat_interval: 1h

receivers:
  - name: "console"

# Exemple mail (à configurer si besoin)
#  - name: "email"
#    email_configs:
#      - to: "ops@example.com"
#        from: "alertmanager@example.com"
#        smarthost: "smtp.example.com:587"
#        auth_username: "alertmanager@example.com"
#        auth_identity: "alertmanager@example.com"
#        auth_password: "VOTRE_MDP"
